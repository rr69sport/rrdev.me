---
import { Image } from 'astro:assets'

interface Props {
  screenshot: string
  altText: string
}

const { screenshot, altText }: Props = Astro.props
---

<Image src={screenshot} alt={altText} width={550} height={100} />
